package challenge1;

public abstract class Shape 
{
    //Create an abstract class Shape with an abstract method calculateArea(). 
    // Implement two subclasses: Circle and Square.
        // Each subclass should have relevant attributes (like radius for Circle, side for Square) and their own  implementation of the calculateArea() method.
    //DECALRED ABSTRACT METHOD
    public abstract void calcualteArea();
}
